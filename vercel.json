{
    "version": 2,
    "builds": [
      {
        "src": "api/main.py",
        "use": "@vercel/python"
      }
    ],
    "routes": [
        { "rewrites": [{ "source": "/(.*)", "destination": "/" }] }
    ]
  }